import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/tab-panel/tab-panel';

export default createComponent(React, 'sl-tab-panel', Component, {});
