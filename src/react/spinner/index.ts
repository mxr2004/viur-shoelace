import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/spinner/spinner';

export default createComponent(React, 'sl-spinner', Component, {});
