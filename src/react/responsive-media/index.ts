import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/responsive-media/responsive-media';

export default createComponent(React, 'sl-responsive-media', Component, {});
