import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/back-to-top/back-to-top';

export default createComponent(React, 'sl-back-to-top', Component, {});
