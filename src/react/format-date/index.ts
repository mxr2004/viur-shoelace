import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/format-date/format-date';

export default createComponent(React, 'sl-format-date', Component, {});
