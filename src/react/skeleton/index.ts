import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/skeleton/skeleton';

export default createComponent(React, 'sl-skeleton', Component, {});
