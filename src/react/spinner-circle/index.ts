import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/spinner-circle/spinner-circle';

export default createComponent(React, 'sl-spinner-circle', Component, {});
