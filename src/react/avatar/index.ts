import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/avatar/avatar';

export default createComponent(React, 'sl-avatar', Component, {});
