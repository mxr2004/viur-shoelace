import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/divider/divider';

export default createComponent(React, 'sl-divider', Component, {});
