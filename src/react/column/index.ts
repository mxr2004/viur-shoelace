import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/column/column';

export default createComponent(React, 'sl-column', Component, {});
