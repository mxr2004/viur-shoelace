import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/breadcrumb-item/breadcrumb-item';

export default createComponent(React, 'sl-breadcrumb-item', Component, {});
