import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import Component from '../../components/markdown-element/markdown-element';

export default createComponent(React, 'sl-markdown-element', Component, {});
